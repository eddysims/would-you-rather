import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { Layout } from ".";

<Meta title="layouts/Layout" component={Layout} />

# Layout

`Layout` is the general layout that will be used on each page.

`Layout` is also where a lot of the application logic will happen.

## Usage

```tsx
import { Layout } from "@/components/Layout";
```

## Props

<ArgsTable component={Layout} />

## Example

<Canvas>
  <Story name="Default">
    {(args) => <Layout {...args}>Children are available</Layout>}
  </Story>
</Canvas>
