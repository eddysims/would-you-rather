import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { Footer } from ".";

<Meta title="footer/Footer" component={Footer} />

# Footer

## Usage

```tsx
import { Footer } from "@/components/Footer";
```

## Props

<ArgsTable component={Footer} />

## Example

<Canvas>
  <Story name="Default">{(args) => <Footer {...args} />}</Story>
</Canvas>

## Get data on scroll

The `Footer` will get its data once it is in the viewport

<Canvas>
  <Story name="Get data on scroll">
    {(args) => (
      <>
        <div style={{ height: "110vh" }}>Scroll to run query</div>
        <Footer {...args} />
      </>
    )}
  </Story>
</Canvas>
