import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";
import { InputText } from ".";

<Meta title="components/InputText" component={InputText} />

# InputText

## Usage

```tsx
import { InputText } from "@/components/InputText";
```

## Props

<ArgsTable component={InputText} />

## Example

<Canvas>
  <Story
    name="Default"
    args={{
      label: "An awesome input",
    }}
  >
    {(args) => <InputText {...args} />}
  </Story>
</Canvas>

## With validations

The `InputText` component uses [React Hook Form](https://react-hook-form.com/) to validate fields.

<Canvas>
  <Story
    name="With validations"
    args={{
      label: "Enter Your Name",
      validations: {
        minLength: {
          value: 3,
          message: "Thats an awfully short name",
        },
        required: "You need a name",
      },
    }}
  >
    {(args) => <InputText {...args} />}
  </Story>
</Canvas>
